# 『下ばっか見てると』/ Mind the Deer — 企画構成案 v0.10

スマホ向けHTML5ゲーム。**16bit風ドット絵の縦スクロール避けゲー**。全100ステージ＋エンドレス。
最終的に iOS App Store へ。

**実装済み**（エンドレス／ステージ100面／端末内ランキング）。コードはこのリポジトリの [`src/`](../src/)。

改訂履歴:
- v0.1 擬似3D＋視点切り替え → 方向性が違ったため破棄
- v0.2 ドット絵の縦スクロールへ全面改稿
- v0.3 横画面案（→ v0.4 で不採用）
- v0.4 縦2段レイアウトに決定。コロコロうんち、グレイズ、ゲームオーバー短縮。**P0実装**
- v0.5 ゲーム画面を横幅いっぱいに（参道13タイル）。**ステージ100面モードと端末内ランキングを実装**。回廊の見た目の露出を修正。道でフンをする鹿を追加
- v0.6 **レベル表示と解禁**、**鹿せんべい売り場と給餌**、**木で道が狭まる**。
  「ぶりぶり鹿が一度も出ない」バグを修正。検証を `npm run verify` に集約
- v0.7 鹿を大幅に増量（群れ・寝ている群れ・餌やり中の観光客）。木を26×30に拡大。
  牡鹿に役割を与える。タッチを**相対方式**に変更
- v0.10 **回廊を廃止。**「置いてから通れるか調べ、通れないぶんだけ取り除く」へ。
  見せかけの道も不要になった（行き止まりが勝手に生まれる）。
  数字をゲーム画面のHUDへ移し、空いた下段にバナー広告。フンの密度を実測して3倍に
- v0.8 **「安全な道が丸見え」を解決**（見せかけの道＝わだち）。
  **鹿せんべいを0から作り直し**（触れれば渡る盾／群れに触れると囲まれる）。
  関所を廃止。仮絵のドット絵を全面差し替え

---

## 0. v0.10 でやったこと：回廊を捨てた

### 3世代かけて隠そうとして、隠しきれなかったもの

v0.4 から v0.9 まで、地形の作り方はずっとこうだった。

```
先に「通れる回廊」を1本引く → その外側にだけ物を置く
```

生成のやり直しが起きず、抜けられない配置も原理的に作られない。良い性質だった。
**ただし副作用として、「正解の道」が常に1本、絵として存在してしまう。**

隠そうとした履歴:

| 版 | 打ち手 | 結果 |
|---|---|---|
| v0.5 | 当たらない小石を回廊の中にも撒く | 輪郭は薄れたが帯は残った |
| v0.5 | 回廊の幅を行ごとに揺らす | 縁がぎざぎざになっただけ |
| v0.7 | 参道いっぱいの「関所」で塞ぎ、回廊だけ穴を開ける | **穴が正解の目印**になり逆効果。廃止 |
| v0.8 | 同じ幅・同じ揺らぎの**見せかけの道を2本**並べる | 効いた。ただし作り物 |
| v0.9 | フンの密度を実測して3倍に | 効いた。それでも参道の44%は安全地帯のまま |

**隠すのが間違いだった。** 予約している限り、予約した場所は必ずどこかにある。

### 入れ替えた

```
好きなだけ置く → 通れるか調べる → 通れないぶんだけ取り除く
```

決められた道は無い。空きは毎行たまたま空いているだけで、分岐もするし行き止まりもする。
**見せかけの道を作る必要が消えた——行き止まりが地形から勝手に生まれる。**

### 仕組み（`route.ts` と `level.ts` の `openRoute`）

1行進むあいだにプレイヤーが横へ動ける量を r とすると

```
次の行の到達範囲 ＝ （いまの到達範囲を左右に r 広げたもの） ∩ （次の行の空き）
```

区間の集合演算だけで済む。これを毎行やって、

- **`s.route`** … まだ辿り着ける範囲ぜんぶ。枝分かれすれば2本以上になる
- **`s.thread`** … その中を実際にたどっている1本。**予約ではなく結果**

`thread` が必ず続くことだけが、このゲームの公平さの保証。
どうしても隙間が無い行だけ、**いちばん撤去の少ない窓**を選んで空ける。
適当な場所を空けると塊の真ん中に穴が空いて人工物に見えるので、
端をわずかに削る形になり、消したことが分からない。

実測（1500m 地点）: 参道の**35%がフンで埋まり**、まだ続く場所は**15%**しかない。
回廊時代（44%が安全地帯・被覆率6%）とは別のゲームになった。

### 実装で踏んだ穴

| 症状 | 原因 |
|---|---|
| 1本に固定しても35mに1回踏む | 当たり判定は**縦6px**あって行の境目をまたぐのに、帯の判定が16px丁度だった |
| 到達範囲を追うボットが死ぬ | いま居る枝が先で行き止まっても、**足元の集合を見ているうちは分からない**。だから証人の1本（thread）が要る |
| 同じ地形で被弾が5回にも0回にもなる | ボットをマウスで操縦していて、往復通信の50〜80msがそのまま操作の遅れになっていた。**地形ではなく計測装置を測っていた** |
| ボットが突っ立ったまま踏まれ続ける | `tx` と `ty` の**両方**が揃わないと1ミリも動かない仕様だった |
| 「安全に歩く」と「縁を攻める」の差が消えた | 保証する隙間をグレイズの届く距離(12px)の2倍より狭くすると、**真ん中を歩くだけで常にかすめる** |

---

## 1. 画面：縦2段

指がゲーム画面にかからないので、キャラが指で隠れる問題が根本的に消える。
v0.3 までの「指のオフセット」設定は不要になったので削除した。

```
┌──────────────┐
│  バナー広告   │  ネイティブ。指の来ない画面最上部にだけ置く
├──────────────┤
│ くつ 12m  340 │  HUD（canvas の上18px）。文字ラベルは使わない
│              │  上段：ゲーム画面（canvas 224×176）。横幅いっぱいに広げる
│   ゲーム画面   │  参道は x 8〜216（208px = 13タイル）
│              │  プレイヤーは y 20〜160
├──────────────┤
│              │
│  操作パッド   │  下段はパッドだけ。**相対方式**（置いた場所からの差分）
│              │  指を置き直してもワープしない。速度は 150px/s で頭打ち
│              │
└──────────────┘
```

### 数字は下段ではなくゲーム画面に置く（v0.9）

最初は下段のDOMに9個の数字を並べていた。実測してみると、**それだけで画面の
1/4（206px）を使っていて、iPhone SE ではパッド(144px)より大きかった**。
しかも避けている最中に画面の下まで目をやる余裕は無く、
下に置いた数字は**ゲームオーバーになってから初めて読まれていた**。

| | 変更前 | 変更後 |
|---|---:|---:|
| iPhone SE のパッド | 144px | **345px** |
| iPhone 14 のパッド | 308px | **491px** |
| 下段のスコア表示 | 206px | 0 |

出す数字も4つに絞った。**くつ（残り）／きょり／スコア／せんべい**だけ。

消したもの（と、その理由）:

| 消した | 理由 |
|---|---|
| ばいりつ「×1.28」 | 見ても次の操作が変わらない。ゲージの絵だけ残した |
| グレイズ数 | 同上。結果画面で見れば足りる |
| たかられた頭数 | **画面に鹿が見えている。**数字は重複した情報 |
| ベスト | プレイ中に比べても仕方がない。タイトルと結果画面へ |
| レベル | きょりから分かるし、上がった瞬間に画面へ一言出している |

**ラベル文字も全部やめた。**「スコア」「きょり」を読ませる設計自体が間違いで、
避けている最中に言葉は読めない。意味は絵（くつ・せんべい）と
大きさ（いちばん大きい数字＝スコア）で示す。

HUD は参道に**重ねず、上に足す**（`HUD_H = 18`、canvas は 224×194）。
鹿は画面の上端から入ってくるので、そこに何か被せると発見が遅れ、
反応時間の下限1.2秒から積み上げた難易度の前提が崩れる。

パッドには指の位置（輪）とキャラの実際の位置（点）を両方出す。
そのずれが「追いついている途中」の手応えになる。

**ゲーム画面は横幅いっぱいに広げる。**左右に余白を作らない。
そのため整数倍ではなく端数倍で拡大するが、スマホは devicePixelRatio が2〜3あるので
実機ではピクセルのムラは見えない。縦は画面高の46%で頭打ち（下段を潰さないため）。

### 横画面を見送った理由

v0.3 で横画面（288×160）を推したが、縦2段のほうが良い。
横は前方視界が短くて「急に現れる」感じは出るが、**速度の上限が5.16タイル/sに縛られる**。
縦2段なら視界11.1タイルを確保しつつ、指はゲーム画面に触らない。両取りできる。

| | 縦2段（採用） | 横画面（v0.3案） |
|---|---:|---:|
| ゲーム画面 | 224×176 | 288×160 |
| 前方視界 | 11.13タイル | 9.38タイル |
| 速度上限 | 6.12 t/s | 5.16 t/s |
| 指が画面にかかる | **かからない** | かかる |

---

## 2. 鹿のフンは丸いコロコロ

1粒 4×4px。実物どおり、落ち方に3種類ある。

| 種類 | 重み | 中身 |
|---|---:|---|
| ばらけ | 0.45 | 2〜4粒を幅30pxに散らす |
| **かたまり** | 0.42 | **8〜12粒**を 13×9px の楕円内に落とす |
| でかいの | 0.13 | 7×7px を1つ。当たり判定は広いが、かすめた見返りも大きい |

置く量は「1行(16px)あたりの塊の数」で決める。
行は毎秒 v 行流れてくるので、**実際の湧き量は v × これ**。
速度が上がるだけで密度も勝手に上がるため、係数自体は緩やかにしか増やさない。

```
poopRate(D) = (1.15 + 1.7·(1 − e^(−D/1400))) × (参道幅 / 128)
            = 1.87 → 4.63 塊/行     ※ 参道が208pxに広がったぶんを補正
```

v0.8 で空けておく帯が1本から3本になったぶん、同じ量では参道がスカスカに見える。
係数を約1.4倍にした。

**道の途中で立ち止まってフンをする鹿**もいる（距離200mから、鹿の22%）。
画面に入ってすぐ止まり、0.75秒かけて7粒落としてから歩き去る。
v0.7 では1.5秒立ち止まっていて、落とし終わる頃には画面の下——
つまり**もう通り過ぎたあと**で、避ける対象になっていなかった。
落ちた粒は**回廊の中にも積もる**——見えているところで起きるので公平で、
しかも後述の「回廊が見えてしまう」問題を自然に崩してくれる。

---

## 3. グレイズ：フンのすぐそばを通ると伸びる

当たり判定の外側 **8px** に入っただけで「かすめた」。1粒につき1回だけ。

```
ゲージ += 0.20（小粒）／ 0.50（でかいの）     上限 2.0
ゲージ *= e^(−0.7·dt)                        指数で減衰
倍率 = 1 + ゲージ                            ×1.0 〜 ×3.0
かすめ点 = 30（小粒）／120（でかいの）× 倍率
被弾すると ゲージ × 0.5
```

**減衰を指数（ゲージ比例）にしているのが肝。**
定数で引くと「稼ぎ続けるか、ゼロか」の二択になって倍率が死ぬ。
実装当初は定数で引いていて、実測したら59秒間ずっと ×1.00 のままだった。

指数なら釣り合い点が

```
ゲージ = 毎秒のグレイズ数 × GAIN ÷ DECAY
```

になり、どの腕前でも意味のある値に落ち着く。目安：毎秒1回で ×1.29、毎秒3回で ×1.86。

被弾でゲージを全没収するのも同じ理由でダメだった（倍率が一生育たない）。半分残す。

---

## 4. ゲームオーバーは早く

汚れ **5** で終了（v0.3 は10）。フン+1、鹿+2。
自動テストのボットで 23〜62m、およそ **15〜25秒** で終わる。上手ければ伸びる。

---

## 5. レベル：変化を見せる

滑らかな曲線で上げるだけだと、上がったことが体感できない。
とくに速度は上限が固定されているので、**600m を過ぎるとほとんど変化しなくなる**。
そこで2つ足した。

1. **レベルを数字で出す**（100mごと）。HUDに常時表示し、上がった瞬間に画面へ一言出す
2. **要素の解禁を段差にする**。数字ではなく「新しいものが出てくる」で進行を見せる

| レベル | 距離 | 出てくるもの |
|---:|---:|---|
| 1 | 0m | フンと鹿 |
| 2 | 100m | よこから鹿がくる ／ **鹿せんべい売り場** |
| 3 | 200m | 鹿が道でしはじめる |
| 4 | 300m | **木で道がせまくなる** |
| 5 | 400m | つのの ある鹿（突進） |
| 6 | 500m | おいかけてくる鹿 |
| 8 | 700m | でかいフンがふえる |

あわせて**フン密度の裾を伸ばした**（時定数 500 → 1400）。
短いと600m付近で頭打ちになり、以降なにも変わらなくなる。

---

## 6. 鹿せんべい：盾であり、拘束時間でもある

たまに**鹿せんべい売り場**が流れてくる（20〜34秒間隔）。通ると**10枚**（実物と同じ束）。
売り場は**回廊の上に置く**ので必ず取りに行ける。危険は取ったあとに来る。

**プレイ中に押すボタンは1つも無い。** 避けるだけで手一杯なので、
すべて「触れたら起きる」に寄せてある。

| 触れたもの | 起きること |
|---|---|
| 単独の鹿 | 1枚渡して無事。0.25秒の無敵。**盾として働く** |
| 群れ（半径46pxに3頭以上） | **囲まれる**。0.5秒に1枚ずつ持っていかれる |
| 牡鹿 | 盾が効かない。餌に興味が無い |

囲まれているあいだは

- 移動速度が **30%** まで落ちる
- 鹿の体でフンが隠れる。見えないまま歩かされる
- **拘束時間＝残り枚数 × 0.5秒**

**盾の枚数がそのまま拘束時間になる**のがこの設計の芯。
「たくさん持っている＝強いが、捕まると長い」という取り引きが1本の数字で決まり、
プレイヤーが売り場に寄るかどうかを毎回考えることになる。

怖さの正体は「ぶつかると痛い」ではなく **「動けない・見えない」**。
囲まれて動けないまま、フンだらけの参道に立たされる——これが怖さの中身で、
今日の奈良で実際に体験したものそのまま。

解けた瞬間は、密着している鹿を外へ26px突き飛ばし、1.3秒の無敵を与える。
これが無いと、解放直後に目の前の数頭に立て続けに轢かれて
**フンを一度も踏んでいないのにゲームオーバー**になる（実際になった）。

---

## 7. 木

レベル4から参道に木が生える。**通れない**。
重なったらいちばん浅い向きへ押し出すので、上から流れてくる木に押されて道が狭まる。

木も回廊は必ず空けて置く。ただし背が18pxあって1行を大きくまたぐため、
**場所を選ぶ時点で「縦のずれぶんの余裕」まで含めて離しておく必要がある**。
これを忘れていて、実装当初は一本も置けていなかった。

---

## 8. 難易度カーブ

上限は**画面の縦幅から逆算する**。反応時間の下限を固定して割り戻すのが順序。

```
D = 進んだ距離 [m]   ※ 1m = 1タイル = 16px

── まず画面から上限を決める ───────────────
前方視界 FIELD = (プレイヤー安全帯のy − 鹿の出現y) / 16 = 11.13 タイル
反応時間の下限 T_min = 1.2 [s]                （設計値・動かさない）
接近速度の上限 = FIELD / T_min = 9.27
  v_max = 0.66 × 9.27 = 6.12      u_max = 0.34 × 9.27 = 3.15

── そのうえでカーブを引く ─────────────────
スクロール速度  v(D) = 2.8 + (6.12 − 2.8)·(1 − e^(−D/450))
鹿の接近速度    u(D) = min(3.15, 0.9 + 0.7·ln(1 + D/200))
フン密度        ρ(D) = 0.8 + 0.5·(1 − e^(−D/500))       [塊/行]
鹿の出現間隔    I(D) = max(0.55, 3.2·e^(−D/800))
追い鹿の割合    h(D) = min(0.45, D/2500)
牡鹿の割合      c(D) = min(0.20, (D−400)/4000)          ※ D > 400 から
```

| 距離 | 速度 | 鹿速度 | 反応時間 | 塊/行 | 鹿間隔 |
|---:|---:|---:|---:|---:|---:|
| 0 m | 2.80 | 0.90 | 3.01s | 0.80 | 3.20s |
| 100 m | 3.46 | 1.18 | 2.39s | 0.89 | 2.82s |
| 250 m | 4.21 | 1.47 | 1.96s | 1.00 | 2.34s |
| 500 m | 5.03 | 1.78 | 1.64s | 1.12 | 1.71s |
| 1000 m | 5.76 | 2.15 | 1.41s | 1.23 | 0.92s |
| ∞ | 6.12 | 3.15 | **1.20s** | 1.30 | 0.55s |

---

## 9. 理不尽にしない仕組み（実装で8回壊れた）

配置してから検査するのではなく、**先に通れる回廊を引いてから、その外側にだけ置く**。
——という方針は v0.2 から変えていないが、実装したら2箇所で破れた。
自動テスト（回廊をなぞるボット）で発見して直した。

### 破れ その1：ずれ幅に理論上限をそのまま使っていた

回廊が1行でずれてよい理論上限は `LATERAL / v`。
プレイヤーが**全速力で横に走ってようやく追いつける**量。
毎行その上限を使うと余裕がゼロになり、追いかけるだけで手一杯になって避ける操作ができない。

```
corridorDrift(D) = DRIFT_SAFETY × LATERAL / v(D)      DRIFT_SAFETY = 0.35
```

### 破れ その2：粒が「別の行」に落ちていた

粒は見た目のために行の基準yから上下にずらして置く。
ところが**ずれているあいだにも回廊は横に動いている**ので、
そのままだと「1行ぶん古い回廊」の位置に粒が乗って道が塞がる。

```
必要な空き = CORRIDOR_HALF + 粒の半幅 + |dy| / 16 × corridorDrift(D)
```

### 破れ その3（見た目の問題）：塊が欠けてスカスカに見えた

「適当に置いて回廊に当たったら捨てる」だと塊がごっそり欠ける。
回廊の左右に残っている帯から先に中心を選ぶようにした（`freeCentre`）。

### 破れ その4：回廊が見えていた（3回直して、3回とも効かなかった）

**いちばん長く残った問題。** 回廊にフンを置かないので、そこだけ綺麗な帯になり、
「正解の道」が絵で丸見えになっていた。避けて歩く遊びが、なぞるだけの作業になる。

隠す方向で三度やって、三度とも効かなかった。

| 打ち手 | 結果 |
|---|---|
| 当たらない小石を回廊の中にも撒く | 帯の輪郭は薄れたが、帯そのものは残った |
| 回廊の幅を行ごとに揺らす | 縁がぎざぎざになっただけ |
| 鹿に回廊でフンをさせる | たまに崩れるだけで、ふだんは空いたまま |
| 参道いっぱいの「関所」で塞ぎ、回廊だけ穴を開ける | **穴が正解の目印になり逆効果**。廃止 |

**隠そうとしたのが間違いだった。** 1本しか無いことが問題なので、
目が慣れれば必ず見つかる。v0.8 で**帯を1本から3本にした**（§0）。
本物と偽物は幅も揺らし方も小石の撒き方も同一で、絵からは区別できない。

偽物は5〜13行で行き止まるが、行き止まりは画面のいちばん上に出る。
プレイヤーは画面のいちばん下にいるので、隣の帯へ移るのに画面1枚ぶんの猶予がある。

### 破れ その5：回廊の中を歩くだけでかすめてしまう

回廊の半幅を揺らすようにしたとき、下限を11pxにしていた。
プレイヤーの判定の半幅4px＋グレイズ範囲8px＝**12px**なので、
半幅が12を割ると安全な線の真ん中を歩くだけでグレイズが入り、攻めと守りの差が消える。
下限を14pxに上げた（その後さらに16pxへ。破れ その7）。

### 破れ その6：偽物を足したら、本物の余裕まで削れていた

見せかけの道を入れたとき、フンを空ける半幅を全帯で固定値14pxにした。
「幅が違えば幅だけで本物が分かる」と考えたためだが、
本物の余裕はもともと 14〜20px の揺らぎがあり、その平均17pxぶんで成り立っていた。
14に固定した結果、**回廊をなぞるボットが129mで3回フンを踏んだ**。

正解は「固定する」ではなく「**同じ範囲で揺らす**」。
偽物にも 14〜20px の揺らぎを持たせ、本物と同じ関数を通すようにした。
これで幅からは何も分からないまま、余裕が戻った。

### 破れ その7：「無傷で走り切れる」が走行ごとに揺れた

上の修正のあとも、回廊をなぞるボットの被弾が **走行ごとに0〜3回**で揺れていた。
平均すれば通るが、**保証が確率になっている時点で保証ではない。**

原因は幅ではなく、フンの寄せ方。
帯の縁をぎざぎざに見せるため、塊の中心を縁のわずか2px外から引くようにしていた
（`EDGE_MARGIN`）。おかげで粒が縁ちょうどまで来る。
半幅14では、線から10pxずれた瞬間に当たる計算で、
人はもちろんボットでもそこまで正確には歩けない。

下限を16pxに上げた。ずれてよい幅が10→12pxになり、揺れが消えた。
**「たまに通る」を「必ず通る」に戻すのに要ったのは2px。**

### 破れ その8：「フンはいっぱいあるのに簡単」——実際は6%しか覆っていなかった

実機を触る前の指摘。**測ってみたら、そのとおりだった。**

| 実測（800m地点） | |
|---|---|
| 参道の幅 ÷ キャラ幅 | 208 ÷ 12 = **17倍**（当たり判定なら26倍） |
| 帯（安全地帯）の占める割合 | 44% |
| **フンが覆っている面積** | **6.0%** |

小さい粒がたくさん散っているので「まみれ」に見えていただけで、
**94%の地面はどこを歩いても当たらなかった。** 帯があるかどうか以前の問題。

ここで大事なのは、**拡大縮小では難しくならない**こと。
キャラも道もフンも一律に2倍にすれば、比率は同じなので難易度は1ミリも動かない。
効くのは次の2つの比だけ:

```
安全な面積の割合  =  帯の幅 × 本数 ÷ 参道の幅
歩ける余地       =  1 − フンの被覆率
```

そこで**密度を距離で伸ばす**形に変えた（`clusterSize` / `scatterSize` を追加）。
定数のまま濃くしたらステージ1がクリア不能になったので、
序盤は小さな落とし物、奥ほど溜まった山、という増え方にしてある。

| 距離 | 被覆率（前 → 後） |
|---:|---|
| 0m | 〜2% → **3.5%** |
| 300m | 〜5% → **11.7%** |
| 800m | **6.0% → 14.3%** |
| 1500m | 〜7% → **27.4%** |

このとき**同時に3つ触って壊した**（回廊をなぞるボットが129mで3回被弾、
ステージ1もクリア不能）。切り分けた結果:

- **ドリフト 0.35→0.5 が主犯。** 回廊が速く動くと、追う側のわずかな遅れが
  そのまま縁への押し付けになる。難しさは密度で作り、ここは触らないと決めた
- `GRAZE_PAD` を 8→6 に下げたら、かすめ帯が8px→6pxに痩せて平均倍率が ×1.18 に落ちた
  （`PELLET_CLEAR_MIN` と連動している。片方だけ動かしてはいけない）

### そのほかの保証

- **予兆**：鹿が画面に入る0.5秒前に、入ってくる辺で印が点滅＋鼻息。牡鹿はさらに0.8秒のため
- **反応時間の下限 1.2秒**。これを固定して速度上限を逆算する
- **無敵時間**：被弾後0.5秒（フン）／1.1秒（鹿）
- **判定は小さめ**：プレイヤーは足元8×6pxのみ
- **逃げ場を作らない**：参道の外へは出られない（縁に植え込みを描いて壁だと分かるように）

### 自動テストの結果

Playwright で3種類のボットを走らせて検証している。

| ボット | 動き | 結果 | フン被弾 | グレイズ/m | 平均倍率 |
|---|---|---|---:|---:|---:|
| corridor | 本物の回廊の中心をなぞる | 40秒**生存**・129m | **1** | 0.35 | ×1.15 |
| **decoy** | **空いて見えるほう（＝偽物）へ寄る** | **死亡** | **5** | — | — |
| graze | 帯の縁を舐める | 死亡 | 4 | **0.67** | ×1.46 |
| blind | 何も見ずに振る | 39mで死亡 | — | — | — |

読み方は3つ。

1. **本物を選べていれば無傷で走り切れる**（公平さの保証そのもの）
2. **絵だけで選ぶと死ぬ**——decoy ボットは corridor ボットと同じ操作の丁寧さで、
   選んだ帯が違うだけで5回踏む。見せかけの道が機能している証拠
3. **縁を舐めれば2倍稼げるが、そのぶん踏む**（攻めと守りの分かれ方）

ステージモードでも同じボットで検証している（ステージ1をクリア）。

### 検証は `npm run verify` にまとめてある

ビルドしてヘッドレスChromiumで実際に遊ばせ、設計が主張している性質を直接測る。
これまでに次を捕まえた。**どれも目視では気づけなかった。**

| 見つかった不具合 | 症状 |
|---|---|
| 回廊が塞がっていた | 回廊をなぞるボットが5回フンを踏んだ |
| 回廊のずれ幅が理論上限だった | 追いかけるだけで手一杯、避ける余裕がゼロ |
| 倍率が一生上がらない | 59秒間ずっと ×1.00（減衰が定数だった） |
| ぶりぶり鹿が出ない | 種類だけ pooper で、落とす粒を0個で作っていた |
| 木が一本も出ない | 場所選びの余裕が、必要なクリアランスにわずかに足りなかった |
| 安全な線でもかすめる | 回廊の下限幅がグレイズ範囲より狭かった |
| フンが6%しか覆っていない | 数は多いのに面積が足りず、94%どこでも歩けた |
| 広告が無いのに復活ボタンが出る | `display:flex` が `hidden` を打ち消していた |
| 偽物を足したら本物の余裕が消えた | 幅を固定値にして、揺らぎぶんの余裕を失っていた（129mで3回被弾） |
| 囲まれたまま死ぬ | 解放時に鹿が密着したままで、フンを踏まずにゲームオーバー |

---

## 10. 出てくるもの

| 要素 | 動き | 当たると |
|---|---|---|
| 鹿のフン（小粒） | 静止 | 汚れ+1、少し滑る、ゲージ半減 |
| 鹿のフン（でかいの） | 静止 | 同上。判定は広いがグレイズの見返りも大きい |
| 鹿（メス・子） | 正面から降りてくる | 汚れ+2、転倒0.3秒、押し戻される |
| 横入り鹿 | 左右の辺から歩いて横切る | 同上 |
| 追い鹿 | 横位置を合わせてくる | 同上。距離1000m前後から増える |
| 牡鹿（角あり） | 0.8秒ためて高速直進 | 同上。距離400mから |
| **道でフンをする鹿** | 画面に入ってすぐ0.75秒立ち止まり、7粒落として歩き去る | 落ちた粒は普通のフンと同じ |
| **群れ** | 3〜6頭が横に並んで降りてくる | 一頭ずつ同じ。まとめて避けられない |
| **寝ている群れ** | 最初から道に寝ている。動かない | 同上。よけて通るしかない |
| **餌やり中の観光客** | 回廊の上を、8頭ほどの鹿に囲まれたまま流れてくる | 触れると自分も囲まれる |
| **木** | 静止 | **通れない**。押し出される |
| **鹿せんべい売り場** | 回廊の上を流れてくる | 通ると**10枚**。盾になるが、囲まれると拘束時間になる |
| 観光客 | ゆっくり同方向へ歩く | 汚れなし。押し戻される。**既定オフ**（観光客モードの駒） |
| 小石 | 静止 | **当たらない**。帯の輪郭を隠すためだけに存在する |

未実装（P2以降）: 水たまり、石灯籠、おまもり

---

## 11. モードとランキング

### ふたつのモード

| | 中身 |
|---|---|
| **ステージ** | 100面。ゴール距離まで歩けばクリア。★で評価され、進むと次が開く |
| **エンドレス** | 距離もフンの量も速度も上がり続ける。端末内ランキングに残る |

難易度の式は**両モードで共通**。ステージ n は「エンドレスの距離 (n−1)×18 m 地点から始める」
と読み替えるだけなので、調整箇所が二重にならない。

```
ステージ n の難易度 = (n − 1) × 18 [m相当]
ステージ n の長さ   = 40 + (n − 1) [m]      1面40m → 100面139m
```

### アンロック

- 各エリアの1面は最初から開いている。2面以降は前の面をクリアすると開く
- **次のエリアは、前のエリアで★15個**（30個中）たまると開く。全部★3を取る必要はない
- ★は 汚れ0 で★★★、1〜2 で★★、3〜4 で★

### ランキング（端末内）

エンドレスの上位10件を localStorage に保存する。**サーバーは使わない**。
スコア・距離・かすめた数を記録し、タイトルとリザルトに出す。
自己ベストと今回の順位が分かればじゅうぶんで、
オンライン化はストア公開後に Game Center を足すほうが早い。

---

## 12. ステージ100の作り方

10エリア × 10ステージ。**チャンク（8〜16行の型）の組み合わせ**で生成する。
型を70個ほど手で作れば100ステージぶんの地形が出る。

| ステージ | エリア | 内容 |
|---|---|---|
| 1–10 | 参道 | チュートリアル。フンのみ → 鹿1頭 |
| 11–20 | 大門 | 柱で通路が狭まる。柱の陰から鹿 |
| 21–30 | 鏡の池 | 水たまり。滑って止まれない |
| 31–40 | 大仏さまの前 | 観光客の群れ。人の流れを読む |
| 41–50 | 若草の坂 | スクロールが速い |
| 51–60 | 石段 | 横移動が段単位に制限される |
| 61–70 | 春日の杜 | 木漏れ日以外が暗い |
| 71–80 | 鹿寄せ広場 | 鹿が大量 |
| 81–90 | 雨の参道 | 水たまり多数。フンと見分けがつかない |
| 91–100 | 夜の奈良 | 暗闇。鹿の目だけが光る |

- 距離は 40m（エリア1）→ 140m（エリア10）
- 各エリアの**10番目はボス**
- 評価は**★3段階**：汚れ0で★★★、1〜2で★★、3〜4で★
- 次エリアの解放は**★15個以上**
- 難易度はエンドレスと**同じ式**を使い、ステージ番号を距離に読み替える

---

## 13. 絵の方針（v0.8 で仮絵を全部差し替え）

素材ファイルは持たない。ドット絵は**文字列の配列**（1文字1ピクセル）で、
27色のパレットを指す。木と背景は計算で描く。

画面は224×176px。1枚あたり十数ピクセルしかないので、
**「細かく描く」より「輪郭とシルエットで一瞬で見分けがつく」を優先**している。
避けゲーは、見分けに0.2秒かかった時点で理不尽になる。

その原則から出てくる決めごと:

| 描くもの | 決めごと | 理由 |
|---|---|---|
| フン | 上を明るく下を黒くした4×4の球 | 砂利（明るい灰）の上で「茶色くて丸くて艶がある」が一目で決まる |
| 小石 | 灰色。輪郭も灰 | 近づけば茶色いフンと確実に見分けられる。**当たらないものが紛らわしいのはいちばん悪い** |
| 牡鹿 | 左右に枝分かれした角で面積を稼ぐ | 盾（せんべい）が効かない唯一の相手。遠くから分からないと理不尽 |
| 寝鹿 | 耳と閉じた目を必ず残す | 潰すとただの茶色い塊になり、避けるものだと伝わらない |
| 観光客 | 紫。他のどれにも使わない色 | 鹿（茶）とも自分（青）とも混ざらない |
| ぶりぶり中 | 上半身は歩いている鹿のまま、脚をたたんで尻尾を上げる | 「**あの鹿が**しゃがんだ」と読める |
| 撒かれたせんべい | 輪郭を黒で締める | 素の色が砂利とほぼ同じ明るさで沈むため |
| 参道の縁 | 縁石1px＋丸い茂みの植え込み | ここから先は歩けない、と絵だけで分かるように |

背景は縦長のストリップを1枚作って2枚重ねで流す。
模様の周期はストリップ高さ(208px)の約数(8)にしてある——継ぎ目で柄が飛ばないように。

---

## 14. 技術構成

- **TypeScript + Canvas 2D + Vite**。`image-rendering: pixelated`、横幅いっぱいに拡大
- 更新は固定タイムステップ1/60秒。描画はrAF
- **素材ファイルはゼロ**。ドット絵は文字列、背景は生成、音は矩形波とノイズの実行時合成
- ビルド出力は index.html + js のみ（**gzip 約10KB**）。完全オフライン
- 保存は localStorage
- **Capacitor** でラップ。`base: "./"` のまま `dist/` を同梱
- 縦画面固定・iPhone先行。レーティング4+
- 実在名は避けて架空の古都に

---

## 15. 広告（App Store 版）

無料で出して広告で回収する。手順は [`IOS.md`](IOS.md)、ここには**なぜその形か**だけ書く。

### バナーは「画面の最上部だけ」に置く

最初は「置く場所が無い」と判断してバナーを外していた。
これは**いまのレイアウトのままでは置けない**という話であって、
レイアウトごと直せる話を、直せない前提で答えていた。誤りだった。

数字をHUDに移して下段が206px空いたので、バナー（実機で50〜60px）は余裕で入り、
**しかもパッドは変更前より広くなる**（iPhone SE で 144→345px、バナーを引いても285px）。

置き場所は**画面のいちばん上**に限る。理由は収益ではなく事故防止:

- 操作パッドは画面の下半分にあり、親指はその中で動く
- パッドの近くにバナーを置くと**誤タップが増える**
- 誤タップは Google に無効なトラフィックと判断され、最悪アカウントごと止まる

参道に重ねる案も、パッドの下に置く案も、この理由で消えている。
なお、ドラッグ中はパッドがポインタを掴んでいるので、
指が上へ流れてもバナーは反応しない。危ないのは「新しく触る」ときだけ。

バナーは**1回遊び終えてから**出す（広告の初期化と同じタイミング）。
起動していきなり広告があると、何のアプリか分かる前に判断されてしまう。

### 主役はリワード動画

```
ゲームオーバー → 「見て つづける」 → 汚れを5から3に戻して再開
```

- プレイヤーが**自分で見ると決めて見る**ので、3つの形式でいちばん単価が高い
- 「もう1回やりたい」という気持ちと、こちらの利害が一致している
- **1回のプレイにつき1度だけ**。無制限にすると、ランキングが
  「動画を何本見たか」の記録になって、記録そのものが無意味になる
- **全快にしない**（5→3）。動画1本でプレイ2回ぶんの価値が出てしまう

### インタースティシャルは3回に1回、かつ2分以上あけて

死んだ瞬間ではなく、**結果画面を閉じるとき**に出す。
毎回出すのは短期の売上を最大にして、長期の売上をゼロにするやり方。

### ATT は起動時に出さない

起動直後に許可を求めても、まだ何のアプリか分からないまま判断させることになり、
ほぼ拒否される。**1回遊び終えてから**出す（`ads.ts` の `initAds` を `finishRun` で呼ぶ）。
拒否されてもパーソナライズなしで広告は出るので、収益はゼロにならない。

### キッズカテゴリには入れない

4+ のレーティングは取るが、Apple の**「キッズ」カテゴリには入れない**。
入れると第三者の広告SDKが事実上使えなくなり、この設計が丸ごと成立しない。

### 広告が無くてもゲームは止まらない

`ads.ts` はネイティブでしか動かず、ブラウザでは全部が無害な空振りになる。
機内モードでも、広告の読み込みに失敗しても、遊びは1ミリも変わらない。
`npm run verify` が広告のことを何も知らないまま通るのは、そのため。

---

## 16. 作る順番

| | 内容 | 状態 |
|---|---|---|
| **P0** | エンドレス — フン＋鹿＋難易度カーブ＋グレイズ＋スコア | **完了** |
| **P1** | ステージ100面・アンロック・端末内ランキング | **完了**（地形は生成のみ） |
| **P2** | ドット絵を本番に差し替え、パレット確定、UI仕上げ | **絵は完了**（UI仕上げが次） |
| **P3** | チャンクによる手づくり地形とエリア固有ギミック、ボス10体 | |
| **P4** | 観光客モード | |
| **P5** | 音・演出・チュートリアル、アシスト設定 | |
| **P6** | Capacitor → TestFlight → 審査 | |

---

*難易度カーブの数値はすべて実計算値。ボットの結果は実測値。*
